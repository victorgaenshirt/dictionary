Fragen:

----
was bedeutet dieses Konstrukt mit super?

public class SortedArrayDictionary<K extends Comparable<? super K>, V> implements Dictionary<K, V> {

genauer gefragt:
warum genuegt nicht:
... <K extends Comparable<K>, ...

----
warum bei insert probleme?

required type: int, provided type K

public V insert(K key, V value) {

     V r = data[key].getValue();

eventuelle Lösung:

Vielleicht brauchen wir eine private Hilfsmethode?
z.B. int searchIndex(K key){}

siehe Kommentare bei search() und insert()
----


while (data.hasNext()) {
    Entry<V, K> tmp = data.next();
    if (tmp.key ==
}
---------------------------------------------------
insert als Pseudocode:

checken, ob key drin ist
falls drin, alten value returnen, neuen eintragen

falls nicht drin
   schleife wieder durchlaufen, checken ob keyToInsert <= vorhandenerKey ist
